import { useState } from "react";
import {Routes, Route, useNavigate} from 'react-router-dom';

function login() {
    const [data,setData]=useState[{userName:"",password:""}]
    return( <div>User is successfully logged in</div> )
}
 
export default login 